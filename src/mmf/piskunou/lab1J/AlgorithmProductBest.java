package mmf.piskunou.lab1J;

public class AlgorithmProductBest extends AlgorithmProductWorst implements Testable {

    public long runner(int iterations)
    {
        matrix[0][0]=0;
        return super.runner(iterations);
    }
}
